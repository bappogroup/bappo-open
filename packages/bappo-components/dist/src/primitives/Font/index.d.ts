import React from 'react';
interface FontProps {
    children?: React.ReactNode;
    fontFamily?: string;
    fontSize?: number;
}
export default function Font({ children, fontFamily, fontSize, }: FontProps): JSX.Element;
export {};
