// @flow

import * as React from 'react';

export type ModalProps = {
  onRequestClose: () => void,
  children?: ?React.Node,
  onOverlayPress?: () => void,
  overlayColor?: string,
  visible?: ?boolean,
};
